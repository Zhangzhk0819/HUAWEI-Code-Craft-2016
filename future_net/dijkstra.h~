#ifndef __DIJKSTRA_H__
#define __DIJKSTRA_H__

#include "config.h"

class Dijkstra
{
	public:
		int dist[maxnode];
		int prev[maxnode];
		int path[maxnode];
						
		void Initialization();
		void ResetPath();
		void ResetDist();
	
		void CalShortestDist(int node_num, int sourcenode, int node_distance[maxnode][maxnode]);
		void SearchPath(int source, int destination);
		void DisplayPath();
									
	private:		
};

#endif
